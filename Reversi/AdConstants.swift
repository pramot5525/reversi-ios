import Foundation

enum AdConstants {
    // MARK: - Test IDs (ใช้ตอน dev เท่านั้น)
    // เปลี่ยนเป็น ID จริงจาก AdMob ก่อน publish ขึ้น App Store

    static let bannerAdUnitID = "ca-app-pub-3591885168954129/1949435781"
}
